#!/usr/bin/env bash
set -euo pipefail

ROOT="/Users/0xvox/multica-ultimate-workbench"
OUTPUT="$ROOT/agents/multica-create-commands.md"

cat > "$OUTPUT" <<'DOC'
# Multica Agent Create Command Templates

Generated by `scripts/generate-create-commands.sh`.

This file is intentionally guarded. It records command templates for creating the Multica workbench agents, but it does not include any unguarded agent creation execution block. Runtime, profile, and workspace IDs below were verified before any workbench agent creation.

## Verified Runtime Exports

Verified on 2026-04-29 from the Desktop Multica profile and `DASH` workspace.

```bash
export MULTICA_PROFILE="desktop-api.multica.ai"
export MULTICA_WORKSPACE_ID="5470ee5d-0791-4713-beb4-fd6a187d6523"

export CLAUDE_RUNTIME_ID="dd23854a-7a38-4d04-8d02-014ba5a9df3d"
export CODEX_RUNTIME_ID="76228a28-203a-4249-9756-731d3cf68554"
export HERMES_RUNTIME_ID="d3a6d5a7-a80e-42ba-9d9e-3cefbc27fcf2"
```

Existing private `Workbench Max` agent on the Codex runtime was observed and must be preserved.

## Runtime ID Verification

Read-only discovery commands:

```bash
multica --profile "$MULTICA_PROFILE" --workspace-id "$MULTICA_WORKSPACE_ID" runtime list --output json
multica --profile "$MULTICA_PROFILE" --workspace-id "$MULTICA_WORKSPACE_ID" agent list --output json
```

Suggested local capture flow:

```bash
mkdir -p /tmp/multica-runtime-discovery
multica --profile "$MULTICA_PROFILE" --workspace-id "$MULTICA_WORKSPACE_ID" runtime list --output json > /tmp/multica-runtime-discovery/runtimes.json
multica --profile "$MULTICA_PROFILE" --workspace-id "$MULTICA_WORKSPACE_ID" agent list --output json > /tmp/multica-runtime-discovery/agents.json
```

## Runtime Variables

| Variable | Intended runtime family | Verification source |
| --- | --- | --- |
| `MULTICA_PROFILE` | Desktop API profile | `desktop-api.multica.ai` profile verification |
| `MULTICA_WORKSPACE_ID` | DASH workspace | explicit profile/workspace `workspace get --output json` |
| `CLAUDE_RUNTIME_ID` | Claude | explicit profile/workspace `runtime list` |
| `CODEX_RUNTIME_ID` | Codex | explicit profile/workspace `runtime list` |
| `HERMES_RUNTIME_ID` | Hermes | explicit profile/workspace `runtime list` |

## Inner Ring

| Agent | Definition file | Runtime variable | Max concurrent tasks | Visibility |
| --- | --- | --- | ---: | --- |
| Workbench Admin | `/Users/0xvox/multica-ultimate-workbench/agents/inner/workbench-admin.md` | `CLAUDE_RUNTIME_ID` | 2 | private |
| Workbench Supervisor | `/Users/0xvox/multica-ultimate-workbench/agents/inner/workbench-supervisor.md` | `CODEX_RUNTIME_ID` or `CLAUDE_RUNTIME_ID` | 2 | private |
| Workbench Synthesizer | `/Users/0xvox/multica-ultimate-workbench/agents/inner/workbench-synthesizer.md` | `HERMES_RUNTIME_ID` or `CLAUDE_RUNTIME_ID` | 1 | private |

## Outer Ring

| Agent | Definition file | Runtime variable | Max concurrent tasks | Visibility |
| --- | --- | --- | ---: | --- |
| Codex Guardian | `/Users/0xvox/multica-ultimate-workbench/agents/outer/codex-guardian.md` | `CODEX_RUNTIME_ID` | 1 | private |
| Codex Developer | `/Users/0xvox/multica-ultimate-workbench/agents/outer/codex-developer.md` | `CODEX_RUNTIME_ID` | 2 | private |
| Hermes Researcher | `/Users/0xvox/multica-ultimate-workbench/agents/outer/hermes-researcher.md` | `HERMES_RUNTIME_ID` | 3 | private |
| Claude Architect | `/Users/0xvox/multica-ultimate-workbench/agents/outer/claude-architect.md` | `CLAUDE_RUNTIME_ID` | 2 | private |
| Claude Docs | `/Users/0xvox/multica-ultimate-workbench/agents/outer/claude-docs.md` | `CLAUDE_RUNTIME_ID` | 2 | private |
| QA Verifier | `/Users/0xvox/multica-ultimate-workbench/agents/outer/qa-verifier.md` | `CODEX_RUNTIME_ID` | 2 | private |
| Benchmark Scout | `/Users/0xvox/multica-ultimate-workbench/agents/outer/benchmark-scout.md` | `HERMES_RUNTIME_ID` or `CODEX_RUNTIME_ID` | 1 | private |
| Ops Mechanic | `/Users/0xvox/multica-ultimate-workbench/agents/outer/ops-mechanic.md` | `CODEX_RUNTIME_ID` | 1 | private |
| Memory Curator | `/Users/0xvox/multica-ultimate-workbench/agents/outer/memory-curator.md` | `CLAUDE_RUNTIME_ID` or `HERMES_RUNTIME_ID` | 1 | private |

## Pilot: Codex Guardian

Use this block first after verifying `CODEX_RUNTIME_ID` from Multica runtime data. It creates only `Codex Guardian`, with Codex approval set to `on-request`, one concurrent task, and private visibility.

```bash
CONFIRM_CREATE_CODEX_GUARDIAN=yes /Users/0xvox/multica-ultimate-workbench/scripts/create-pilot-agent.sh
```

The script refuses to contact Multica unless `CONFIRM_CREATE_CODEX_GUARDIAN=yes` is set at action time and the effective `MULTICA_PROFILE`, `MULTICA_WORKSPACE_ID`, and `CODEX_RUNTIME_ID` exactly match the verified pilot target values below.

Equivalent command shape:

```bash
CANONICAL_MULTICA_PROFILE="desktop-api.multica.ai"
CANONICAL_MULTICA_WORKSPACE_ID="5470ee5d-0791-4713-beb4-fd6a187d6523"
CANONICAL_CODEX_RUNTIME_ID="76228a28-203a-4249-9756-731d3cf68554"

MULTICA_PROFILE="${MULTICA_PROFILE:-$CANONICAL_MULTICA_PROFILE}"
MULTICA_WORKSPACE_ID="${MULTICA_WORKSPACE_ID:-$CANONICAL_MULTICA_WORKSPACE_ID}"
CODEX_RUNTIME_ID="${CODEX_RUNTIME_ID:-$CANONICAL_CODEX_RUNTIME_ID}"
ROOT="/Users/0xvox/multica-ultimate-workbench"

[[ "$MULTICA_PROFILE" == "$CANONICAL_MULTICA_PROFILE" ]]
[[ "$MULTICA_WORKSPACE_ID" == "$CANONICAL_MULTICA_WORKSPACE_ID" ]]
[[ "$CODEX_RUNTIME_ID" == "$CANONICAL_CODEX_RUNTIME_ID" ]]

multica --profile "$MULTICA_PROFILE" --workspace-id "$MULTICA_WORKSPACE_ID" agent create \
  --name "Codex Guardian" \
  --description "High-risk local ops, command approval, rollback planning, and evidence-first verification." \
  --instructions "$(cat "$ROOT/agents/outer/codex-guardian.md")" \
  --runtime-id "$CODEX_RUNTIME_ID" \
  --runtime-config '{"custom_args":["--ask-for-approval","on-request"]}' \
  --max-concurrent-tasks 1 \
  --visibility private \
  --output json
```

Before expanding beyond the pilot:

```bash
multica --profile "$MULTICA_PROFILE" --workspace-id "$MULTICA_WORKSPACE_ID" agent list --output json
```
DOC

printf 'Generated %s\n' "$OUTPUT"
